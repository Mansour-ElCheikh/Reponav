/**
 * Webview types — re-exported from the shared source of truth.
 */
export {
    FileCategory,
    TourType,
    CodeHighlight,
    FileRelationship,
    TourStep,
    GraphNode,
    GraphEdge,
    TourGraph,
    Tour,
    AnalysisReportSummary as AnalysisReport,
    AppConfig,
    ExtensionToWebviewMessage,
    WebviewToExtensionMessage,
    FlowStep,
    FlowAnomaly,
    FlowSequence,
} from '../../shared/types';
